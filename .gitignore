# colcon build artifacts
build/
install/
log/

# core dumps
core
core.*

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ML model weights (large binaries — distribute out of band)
*.pt
*.onnx
*.engine
*_ncnn_model/

# editor / os
.vscode/
.idea/
.DS_Store

# Full-desktop grabs from scripts/capture_arm_poses.py. The compositor has no
# per-window capture here, so these frame the whole screen, terminal included.
# Commit the cropped viewport (runs/shots/crop-*.jpg), never these.
runs/shots/*.png
